# Application Port (8001 used to avoid conflicts if 8000 is occupied)
PORT=8001

# Neon Database Connection String (PostgreSQL compatible)
# Format: postgresql://[user]:[password]@[neon-hostname]/[database_name]?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/appointment_board?sslmode=require"
